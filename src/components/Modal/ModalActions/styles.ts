import { createStyles } from '@mui/styles'
import { Theme } from '@mui/material/styles'


export const ModalActions = (theme: Theme) => createStyles({
  root: {
    margin: 0,
    padding: theme.spacing(3)
  }
})
